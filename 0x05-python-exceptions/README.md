# PYTHON EXCEPTIONS

Computer programs are bound to produce errors. Some of these errors are due to failure to use the correct syntax of a language [SYNTAX ERROR] and the second tror occurs when syntactically correct code produces errors at Runtime[EXCEPTIONS].

Unhandled exceptions may lead to the premature and abrupt termination of a program, hence the need for proper exception handling
